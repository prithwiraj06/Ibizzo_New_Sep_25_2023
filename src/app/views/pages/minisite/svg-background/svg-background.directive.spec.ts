import { SvgBackgroundDirective } from './svg-background.directive';

describe('SvgBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
