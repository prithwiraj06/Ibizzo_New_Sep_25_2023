import { CharnameDirective } from './charname.directive';

describe('CharnameDirective', () => {
  it('should create an instance', () => {
    const directive = new CharnameDirective();
    expect(directive).toBeTruthy();
  });
});
