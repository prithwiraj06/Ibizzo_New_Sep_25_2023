import { HyperlinkDirective } from './hyperlink.directive';

describe('HyperlinkDirective', () => {
  it('should create an instance', () => {
    const directive = new HyperlinkDirective();
    expect(directive).toBeTruthy();
  });
});
